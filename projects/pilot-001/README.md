# 首条样片（Pilot 001）

首条约两分钟的 16:9 中文样片。当前文件是待审草案，不会自动触发付费生成。

顺序：

1. 确认 `script.md` 的内容和表达。
2. 放入本地人物照片，通过母图门槛。
3. 生成 10–15 秒嘴型/声音测试。
4. 确认 `strategy.md` 后制作 720p 预览。
5. 预览通过后渲染 1080p 成片。

本地素材目录 `inputs/`、剪辑目录 `edit/`、输出目录 `outputs/` 被 Git 忽略。
